@*@model PLnew.Models.RecordViewModel

<div class="modal fade" id="modal-1">
    <div class="modal-dialog modal-sm">
        <div class="modal-content">
            <div class="modal-header">
                <button class="close" type="button" data-dismiss="modal">
                    &times;
                </button>
            </div>
            <div class="modal-body">
              @using (Html.BeginForm("EditModalREcord", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
                    <div class="form-group ">
                       
                        <div class="col-md-12 textArea">
                            @Html.TextArea("mod", "", new { @id = "name", @class = "form-control", @placeholder = "What's happening ?" })
                        </div>
                        <div class="col-md-12 textArea">
                            @Html.TextBox("theme", "", new { @id = "theme", @class = "form-control", @placeholder = "Theme?" })
                        </div>
                    </div>
                    <div class="form-group">
                        <div class="col-md-12">
                            <button type="submit" class="btn btn-default" id="res"> Edit</button>
                        </div>

                    </div>
                }
            </div>
            <div class="modal-footer">
                <button class="btn btn-danger" type="button" data-dismiss="modal">Close</button>
            </div>
        </div>
    </div>
</div>*@